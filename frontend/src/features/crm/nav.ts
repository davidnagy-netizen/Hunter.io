import { TargetIcon } from "@/shared/components";
import type { NavItem } from "@/shared/types/navigation.types";

export const crmNav: NavItem[] = [{ to: "/admin/crm", labelKey: "nav.crm", namespace: "crm", icon: TargetIcon }];
